# - 
#   condition:
#     clauses:
#       - literals:
#           - check: "T"
#             condition_to_check:
#               name: "payload_in"
#               pddl_type: 2
#               params:
#                 - "{payload_x}"
#                 - "{wp_to}"
              
#           - check: "T"
#             condition_to_check:
#               name: "payload_should_be_in"
#               pddl_type: 2
#               params:
#                 - "{payload_x}"
#                 - "{tool_x}"
#                 - "{wp_to}"
              

#   reactive_rules:
#     - set: belief
#       operation: DEL
#       value:
#         name: "payload_should_be_in"
#         pddl_type: 2
#         params:
#           - "{payload_x}"
#           - "{tool_x}"
#           - "{wp_to}"
- 
  condition:
    clauses:
      - literals:
          - check: "F"
            condition_to_check:
              name: "payload_in"
              pddl_type: 2
              params:
                - "{payload_x}"
                - "{wp_to}"
              
          - check: "T"
            condition_to_check:
              name: "payload_should_be_in"
              pddl_type: 2
              params:
                - "{payload_x}"
                - "{wp_to}"
                - "{tool_x}"
                - "{num_bots}"
              

  reactive_rules:
    - set: desire
      operation: ADD
      value:
        name: "transport_payload"
        priority: 0.8
        deadline: 50.0
        value:
          - name: "payload_in"                   # changed move_payload_to, I think it's redundant to payload_should_be_in
            pddl_type: 2
            params: 
              - "{payload_x}"
              - "{wp_to}"